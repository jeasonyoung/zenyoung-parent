package ${packageName}.dao.jpa;

import ${packageName}.dao.entity.${className}Entity;
import top.zenyoung.data.jpa.JpaBase;

/**
 * ${comment!}-jpa
 * <#assign lastTime = .now>
 * @author ${author!}
 * @version 1.0
 * @date ${lastTime?string('yyyy-MM-dd HH:mm:ss')}
 **/
public interface ${className}Jpa extends JpaBase<${className}Entity, Long> {

}
