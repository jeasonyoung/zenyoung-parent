spring:
  application:
    name: code-generator
  webflux:
    date-format: yyyy-MM-dd HH:mm:ss
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  thymeleaf:
    cache: false
    mode: HTML5
logging:
  level:
    org.springframework.scheduling: 'INFO'
